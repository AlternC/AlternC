<Virtualhost *:80> 
  ServerName %%fqdn%%
  AssignUserId #%%UID%% #%%GID%% 
  SetEnv LOGIN "%%UID%%-%%LOGIN%%"

  KeepAlive Off 

  RewriteEngine On 
  RewriteRule ^/(.*)$ %%redirect%%/$1 [R=301,L] 

</Virtualhost>
